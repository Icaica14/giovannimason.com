---
import Base from '../../../layouts/Base.astro';
import NewsList from '../../../components/NewsList.astro';
import { t } from '../../../i18n/ui';

const lang = 'en' as const;
---

<Base
  lang={lang}
  currentRoute="news"
  title={t(lang, 'news.meta.title')}
  description={t(lang, 'news.meta.description')}
>
  <NewsList lang={lang} />
</Base>
