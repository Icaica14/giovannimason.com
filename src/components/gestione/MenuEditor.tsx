/**
 * Editor del menu — voci, prezzi, descrizioni IT/EN per sezione.
 * Placeholder di Fase 1: l'editing completo arriva in Fase 4 insieme a
 * menuRemote.ts e alla Edge Function trigger-rebuild.
 */
export default function MenuEditor() {
  return (
    <section>
      <h2 class="g-h2">Menu</h2>
      <p class="g-sub">Aggiungi, modifica o rimuovi voci e prezzi per ogni sezione.</p>
      <div class="g-empty">Editor del menu in arrivo.</div>
    </section>
  );
}
