/**
 * Editor degli eventi — CRUD + upload locandina sul bucket "posters".
 * Placeholder di Fase 1: l'editing completo arriva in Fase 5 insieme a
 * eventiRemote.ts e alla migrazione di EventiList + BookingForm.
 */
export default function EventsEditor() {
  return (
    <section>
      <h2 class="g-h2">Eventi</h2>
      <p class="g-sub">Crea e modifica gli eventi con locandina.</p>
      <div class="g-empty">Editor degli eventi in arrivo.</div>
    </section>
  );
}
